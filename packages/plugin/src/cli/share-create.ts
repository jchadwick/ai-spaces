export async function createShare(spaceId: string, options: { role: string; expires: string }) {
  console.log(`Create share for ${spaceId} with role ${options.role}, expires ${options.expires}: not implemented`);
}